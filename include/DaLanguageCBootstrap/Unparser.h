#ifndef DALCBOOT_UNPARSER
#define DALCBOOT_UNPARSER

#include "DaLanguageCBootstrap/Tokenizer.h"
#include "DaLanguageCBootstrap/PT.h"

Token unparseToken(PTNode** curNode);

#endif //DALCBOOT_UNPARSER
